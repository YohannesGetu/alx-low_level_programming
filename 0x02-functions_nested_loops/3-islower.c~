#include "holberton.h"

/**
 * _islower - if the character is lowercase
 *
 * Return: 1 if lowercase character 0 if not
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
