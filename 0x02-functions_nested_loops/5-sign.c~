#include "holberton.h"

/**
 * _isalpha - chackes if the argument is alpahbetic
 * @c:  is the int that will use for the argument of the function
 * Return: 1 if alpabetic; 0 if not
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
		return (1);
	else
		return (0);
}
