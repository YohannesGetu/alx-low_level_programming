#include "holberton.h"

/**
 * print_line - draws a straight line in the terminal
 * @n: intiger agrument
 * Return: void
 */
void print_line(int n)
{
	if (n > 0)
	{
		while (num > 0)
		{
			_putchar('_');
			num--;
		}
	}
	_putchar('\n');
}
