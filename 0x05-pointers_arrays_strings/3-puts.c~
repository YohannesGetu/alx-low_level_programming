#include "holberton.h"

/**
 * _puts - prints a string, followed by a new line, to stdout
 * @str: pointer of char
 * Return: void
 */
void _puts(char *str)
{
	int i = 0;

	while (str[i])
	{
		_puts(str[i]);
		i++;
	}

	_puts('\n');
}
